Talks: rebuild timeline; sharpen research interests

Talks
- Replace ad-hoc negative-offset markers with a proper rail-and-marker
  layout: a single rail span with markers centered on it via
  -translate-x-1/2 and bg-bg punching a clean hole. Fixes the visual
  drift where icons sat off the timeline line.
- Shorten verbose `type` strings (e.g., "Conference paper presentation"
  -> "Paper") so the uppercase-tracked badge no longer wraps awkwardly.
- Promote DVCON's "1st Runner-Up" into its own award badge with a
  trophy glyph alongside the type pill.
- Move the date to the right edge of the badge row, add a short summary
  paragraph per talk so each entry stands on its own.

Research interests
- Replace 4 high-level phrases ("ML accelerators", "Pipelined RISC-V
  microarchitecture") with 6 concrete items grounded in actual work:
  ViT/CNN systolic arrays over AXI4, RV64I + Zba with SVA verification,
  Booth + compressor + Kogge-Stone CLAs in Cadence, HDC valid/ready
  pipelines, RCX-aware closure, Genesys 2 / Kintex-7 prototyping.
- Loosen the Hero interests grid spacing so longer items breathe.
